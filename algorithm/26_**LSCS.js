const LSCS = function (arr) {
    //TODO: 여기에 코드를 작성합니다.
    let result= [];

    let tempArr = [];
    for (let i=0; i<arr.length; i++) { 
        tempArr.push(arr[i]);
        result.push(tempArr)
      
    }
    
    return result;
  };
